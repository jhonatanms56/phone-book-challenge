spring:
  jpa:
    properties:
      show-sql: true
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL95Dialect
        ddl:
          auto: none
        jdbc:
          lob:
            non_contextual_creation: true
  datasource:
    url: jdbc:mysql://phonebook.cdgpan8yrysq.us-east-1.rds.amazonaws.com:3306/phonebook
    username: admin
    password: Desarrollo1
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false

server:
  error:
    whitelabel:
      enabled: false

